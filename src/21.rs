fn main() {
    // Example usage of Rust functions and structures
    let number = 10;
    let squared_number = number * number; // Squaring the number
    println!("The square of {} is {}", number, squared_number);
}
